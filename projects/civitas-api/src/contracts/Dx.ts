/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'
import { AppFactory as _AppFactory, AppFactoryAppClientParams, AppFactoryResolveAppClientByCreatorAndNameParams, AppFactoryDeployParams, AppFactoryParams, CreateSchema } from '@algorandfoundation/algokit-utils/types/app-factory'
import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'
import SimulateResponse = modelsv2.SimulateResponse

export const APP_SPEC: Arc56Contract = {"name":"Dx","structs":{},"methods":[{"name":"whitelist_organisation","args":[{"type":"address","name":"address"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"remove_organisation","args":[{"type":"address","name":"address"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"events":[],"recommendations":{}},{"name":"is_whitelisted","args":[{"type":"address","name":"address"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}},{"name":"get_org_count","args":[],"returns":{"type":"uint64"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":1},"local":{"ints":0,"bytes":0}},"keys":{"global":{"owner":{"keyType":"AVMString","valueType":"address","key":"b3duZXI="},"org_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"b3JnX2NvdW50"}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"whitelisted":{"keyType":"address","valueType":"uint64","prefix":"d2xf"}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[125],"errorMessage":"Already whitelisted"},{"pc":[163],"errorMessage":"Not whitelisted"},{"pc":[155],"errorMessage":"Only the owner can remove"},{"pc":[116],"errorMessage":"Only the owner can whitelist"},{"pc":[129,198],"errorMessage":"check self.org_count exists"},{"pc":[114,153],"errorMessage":"check self.owner exists"},{"pc":[108,147,175],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"source":{"approval":"","clear":"I3ByYWdtYSB2ZXJzaW9uIDExCiNwcmFnbWEgdHlwZXRyYWNrIGZhbHNlCgovLyBhbGdvcHkuYXJjNC5BUkM0Q29udHJhY3QuY2xlYXJfc3RhdGVfcHJvZ3JhbSgpIC0+IHVpbnQ2NDoKbWFpbjoKICAgIHB1c2hpbnQgMQogICAgcmV0dXJuCg=="},"byteCode":{"approval":"","clear":"C4EBQw=="},"compilerInfo":{"compiler":"puya","compilerVersion":{"major":5,"minor":7,"patch":1}},"events":[],"templateVariables":{}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the Dx contract
 */
export type DxArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'whitelist_organisation(address)void': {
      address: string
    }
    'remove_organisation(address)void': {
      address: string
    }
    'is_whitelisted(address)bool': {
      address: string
    }
    'get_org_count()uint64': Record<string, never>
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'whitelist_organisation(address)void': [address: string]
    'remove_organisation(address)void': [address: string]
    'is_whitelisted(address)bool': [address: string]
    'get_org_count()uint64': []
  }
}

/**
 * The return type for each method
 */
export type DxReturns = {
  'whitelist_organisation(address)void': void
  'remove_organisation(address)void': void
  'is_whitelisted(address)bool': boolean
  'get_org_count()uint64': bigint
}

/**
 * Defines the types of available calls and state of the Dx smart contract.
 */
export type DxTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'whitelist_organisation(address)void' | 'whitelist_organisation', {
      argsObj: DxArgs['obj']['whitelist_organisation(address)void']
      argsTuple: DxArgs['tuple']['whitelist_organisation(address)void']
      returns: DxReturns['whitelist_organisation(address)void']
    }>
    & Record<'remove_organisation(address)void' | 'remove_organisation', {
      argsObj: DxArgs['obj']['remove_organisation(address)void']
      argsTuple: DxArgs['tuple']['remove_organisation(address)void']
      returns: DxReturns['remove_organisation(address)void']
    }>
    & Record<'is_whitelisted(address)bool' | 'is_whitelisted', {
      argsObj: DxArgs['obj']['is_whitelisted(address)bool']
      argsTuple: DxArgs['tuple']['is_whitelisted(address)bool']
      returns: DxReturns['is_whitelisted(address)bool']
    }>
    & Record<'get_org_count()uint64' | 'get_org_count', {
      argsObj: DxArgs['obj']['get_org_count()uint64']
      argsTuple: DxArgs['tuple']['get_org_count()uint64']
      returns: DxReturns['get_org_count()uint64']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        owner: string
        orgCount: bigint
      }
      maps: {}
    }
    box: {
      keys: {}
      maps: {
        whitelisted: Map<string, bigint>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type DxSignatures = keyof DxTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type DxNonVoidMethodSignatures = keyof DxTypes['methods'] extends infer T ? T extends keyof DxTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the Dx smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends DxSignatures> = DxTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the Dx smart contract to the method's return type
 */
export type MethodReturn<TSignature extends DxSignatures> = DxTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = DxTypes['state']['global']['keys']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = DxTypes['state']['box']['keys']


/**
 * Defines supported create method params for this smart contract
 */
export type DxCreateCallParams =
  | Expand<AppClientBareCallParams & {method?: never} & {onComplete?: OnApplicationComplete.NoOpOC} & CreateSchema>
/**
 * Defines arguments required for the deploy method.
 */
export type DxDeployParams = Expand<Omit<AppFactoryDeployParams, 'createParams' | 'updateParams' | 'deleteParams'> & {
  /**
   * Create transaction parameters to use if a create needs to be issued as part of deployment; use `method` to define ABI call (if available) or leave out for a bare call (if available)
   */
  createParams?: DxCreateCallParams
}>


/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the Dx smart contract
 */
export abstract class DxParamsFactory {
  /**
   * Constructs a no op call for the whitelist_organisation(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static whitelistOrganisation(params: CallParams<DxArgs['obj']['whitelist_organisation(address)void'] | DxArgs['tuple']['whitelist_organisation(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'whitelist_organisation(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.address],
    }
  }
  /**
   * Constructs a no op call for the remove_organisation(address)void ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static removeOrganisation(params: CallParams<DxArgs['obj']['remove_organisation(address)void'] | DxArgs['tuple']['remove_organisation(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'remove_organisation(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.address],
    }
  }
  /**
   * Constructs a no op call for the is_whitelisted(address)bool ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static isWhitelisted(params: CallParams<DxArgs['obj']['is_whitelisted(address)bool'] | DxArgs['tuple']['is_whitelisted(address)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'is_whitelisted(address)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.address],
    }
  }
  /**
   * Constructs a no op call for the get_org_count()uint64 ABI method
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static getOrgCount(params: CallParams<DxArgs['obj']['get_org_count()uint64'] | DxArgs['tuple']['get_org_count()uint64']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'get_org_count()uint64' as const,
      args: Array.isArray(params.args) ? params.args : [],
    }
  }
}

/**
 * A factory to create and deploy one or more instance of the Dx smart contract and to create one or more app clients to interact with those (or other) app instances
 */
export class DxFactory {
  /**
   * The underlying `AppFactory` for when you want to have more flexibility
   */
  public readonly appFactory: _AppFactory

  /**
   * Creates a new instance of `DxFactory`
   *
   * @param params The parameters to initialise the app factory with
   */
  constructor(params: Omit<AppFactoryParams, 'appSpec'>) {
    this.appFactory = new _AppFactory({
      ...params,
      appSpec: APP_SPEC,
    })
  }
  
  /** The name of the app (from the ARC-32 / ARC-56 app spec or override). */
  public get appName() {
    return this.appFactory.appName
  }
  
  /** The ARC-56 app spec being used */
  get appSpec() {
    return APP_SPEC
  }
  
  /** A reference to the underlying `AlgorandClient` this app factory is using. */
  public get algorand(): AlgorandClient {
    return this.appFactory.algorand
  }
  
  /**
   * Returns a new `AppClient` client for an app instance of the given ID.
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public getAppClientById(params: AppFactoryAppClientParams) {
    return new DxClient(this.appFactory.getAppClientById(params))
  }
  
  /**
   * Returns a new `AppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public async getAppClientByCreatorAndName(
    params: AppFactoryResolveAppClientByCreatorAndNameParams,
  ) {
    return new DxClient(await this.appFactory.getAppClientByCreatorAndName(params))
  }

  /**
   * Idempotently deploys the Dx smart contract.
   *
   * @param params The arguments for the contract calls and any additional parameters for the call
   * @returns The deployment result
   */
  public async deploy(params: DxDeployParams = {}) {
    const result = await this.appFactory.deploy({
      ...params,
    })
    return { result: result.result, appClient: new DxClient(result.appClient) }
  }

  /**
   * Get parameters to create transactions (create and deploy related calls) for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the Dx smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The params for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.params.bare.create(params)
      },
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the Dx smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The transaction for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.createTransaction.bare.create(params)
      },
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the Dx smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The create result
       */
      bare: async (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        const result = await this.appFactory.send.bare.create(params)
        return { result: result.result, appClient: new DxClient(result.appClient) }
      },
    },

  }

}
/**
 * A client to make calls to the Dx smart contract
 */
export class DxClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `DxClient`
   *
   * @param appClient An `AppClient` instance which has been created with the Dx app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `DxClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }
  
  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends DxNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }
  
  /**
   * Returns a new `DxClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<DxClient> {
    return new DxClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `DxClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<DxClient> {
    return new DxClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the Dx smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the Dx smart contract using the `whitelist_organisation(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    whitelistOrganisation: (params: CallParams<DxArgs['obj']['whitelist_organisation(address)void'] | DxArgs['tuple']['whitelist_organisation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DxParamsFactory.whitelistOrganisation(params))
    },

    /**
     * Makes a call to the Dx smart contract using the `remove_organisation(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    removeOrganisation: (params: CallParams<DxArgs['obj']['remove_organisation(address)void'] | DxArgs['tuple']['remove_organisation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DxParamsFactory.removeOrganisation(params))
    },

    /**
     * Makes a call to the Dx smart contract using the `is_whitelisted(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    isWhitelisted: (params: CallParams<DxArgs['obj']['is_whitelisted(address)bool'] | DxArgs['tuple']['is_whitelisted(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(DxParamsFactory.isWhitelisted(params))
    },

    /**
     * Makes a call to the Dx smart contract using the `get_org_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    getOrgCount: (params: CallParams<DxArgs['obj']['get_org_count()uint64'] | DxArgs['tuple']['get_org_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.params.call(DxParamsFactory.getOrgCount(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the Dx smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the Dx smart contract using the `whitelist_organisation(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    whitelistOrganisation: (params: CallParams<DxArgs['obj']['whitelist_organisation(address)void'] | DxArgs['tuple']['whitelist_organisation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DxParamsFactory.whitelistOrganisation(params))
    },

    /**
     * Makes a call to the Dx smart contract using the `remove_organisation(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    removeOrganisation: (params: CallParams<DxArgs['obj']['remove_organisation(address)void'] | DxArgs['tuple']['remove_organisation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DxParamsFactory.removeOrganisation(params))
    },

    /**
     * Makes a call to the Dx smart contract using the `is_whitelisted(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    isWhitelisted: (params: CallParams<DxArgs['obj']['is_whitelisted(address)bool'] | DxArgs['tuple']['is_whitelisted(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(DxParamsFactory.isWhitelisted(params))
    },

    /**
     * Makes a call to the Dx smart contract using the `get_org_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    getOrgCount: (params: CallParams<DxArgs['obj']['get_org_count()uint64'] | DxArgs['tuple']['get_org_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      return this.appClient.createTransaction.call(DxParamsFactory.getOrgCount(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the Dx smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the Dx smart contract using the `whitelist_organisation(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    whitelistOrganisation: async (params: CallParams<DxArgs['obj']['whitelist_organisation(address)void'] | DxArgs['tuple']['whitelist_organisation(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DxParamsFactory.whitelistOrganisation(params))
      return {...result, return: result.return as unknown as (undefined | DxReturns['whitelist_organisation(address)void'])}
    },

    /**
     * Makes a call to the Dx smart contract using the `remove_organisation(address)void` ABI method.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    removeOrganisation: async (params: CallParams<DxArgs['obj']['remove_organisation(address)void'] | DxArgs['tuple']['remove_organisation(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DxParamsFactory.removeOrganisation(params))
      return {...result, return: result.return as unknown as (undefined | DxReturns['remove_organisation(address)void'])}
    },

    /**
     * Makes a call to the Dx smart contract using the `is_whitelisted(address)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    isWhitelisted: async (params: CallParams<DxArgs['obj']['is_whitelisted(address)bool'] | DxArgs['tuple']['is_whitelisted(address)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(DxParamsFactory.isWhitelisted(params))
      return {...result, return: result.return as unknown as (undefined | DxReturns['is_whitelisted(address)bool'])}
    },

    /**
     * Makes a call to the Dx smart contract using the `get_org_count()uint64` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    getOrgCount: async (params: CallParams<DxArgs['obj']['get_org_count()uint64'] | DxArgs['tuple']['get_org_count()uint64']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC} = {args: []}) => {
      const result = await this.appClient.send.call(DxParamsFactory.getOrgCount(params))
      return {...result, return: result.return as unknown as (undefined | DxReturns['get_org_count()uint64'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new DxClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the Dx smart contract using the `is_whitelisted(address)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async isWhitelisted(params: CallParams<DxArgs['obj']['is_whitelisted(address)bool'] | DxArgs['tuple']['is_whitelisted(address)bool']>) {
    const result = await this.appClient.send.call(DxParamsFactory.isWhitelisted(params))
    return result.return as unknown as DxReturns['is_whitelisted(address)bool']
  }

  /**
   * Makes a readonly (simulated) call to the Dx smart contract using the `get_org_count()uint64` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async getOrgCount(params: CallParams<DxArgs['obj']['get_org_count()uint64'] | DxArgs['tuple']['get_org_count()uint64']> = {args: []}) {
    const result = await this.appClient.send.call(DxParamsFactory.getOrgCount(params))
    return result.return as unknown as DxReturns['get_org_count()uint64']
  }

  /**
   * Methods to access state for the current Dx app
   */
  state = {
    /**
     * Methods to access global state for the current Dx app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          owner: result.owner,
          orgCount: result.org_count,
        }
      },
      /**
       * Get the current value of the owner key in global state
       */
      owner: async (): Promise<string | undefined> => { return (await this.appClient.state.global.getValue("owner")) as string | undefined },
      /**
       * Get the current value of the org_count key in global state
       */
      orgCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("org_count")) as bigint | undefined },
    },
    /**
     * Methods to access box state for the current Dx app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the whitelisted map in box state
       */
      whitelisted: {
        /**
         * Get all current values of the whitelisted map in box state
         */
        getMap: async (): Promise<Map<string, bigint>> => { return (await this.appClient.state.box.getMap("whitelisted")) as Map<string, bigint> },
        /**
         * Get a current value of the whitelisted map by key from box state
         */
        value: async (key: string): Promise<bigint | undefined> => { return await this.appClient.state.box.getMapValue("whitelisted", key) as bigint | undefined },
      },
    },
  }

  public newGroup(): DxComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a whitelist_organisation(address)void method call against the Dx contract
       */
      whitelistOrganisation(params: CallParams<DxArgs['obj']['whitelist_organisation(address)void'] | DxArgs['tuple']['whitelist_organisation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.whitelistOrganisation(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a remove_organisation(address)void method call against the Dx contract
       */
      removeOrganisation(params: CallParams<DxArgs['obj']['remove_organisation(address)void'] | DxArgs['tuple']['remove_organisation(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.removeOrganisation(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a is_whitelisted(address)bool method call against the Dx contract
       */
      isWhitelisted(params: CallParams<DxArgs['obj']['is_whitelisted(address)bool'] | DxArgs['tuple']['is_whitelisted(address)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.isWhitelisted(params)))
        resultMappers.push((v) => client.decodeReturnValue('is_whitelisted(address)bool', v))
        return this
      },
      /**
       * Add a get_org_count()uint64 method call against the Dx contract
       */
      getOrgCount(params: CallParams<DxArgs['obj']['get_org_count()uint64'] | DxArgs['tuple']['get_org_count()uint64']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.getOrgCount(params)))
        resultMappers.push((v) => client.decodeReturnValue('get_org_count()uint64', v))
        return this
      },
      /**
       * Add a clear state call to the Dx contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as DxComposer
  }
}
export type DxComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the whitelist_organisation(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  whitelistOrganisation(params?: CallParams<DxArgs['obj']['whitelist_organisation(address)void'] | DxArgs['tuple']['whitelist_organisation(address)void']>): DxComposer<[...TReturns, DxReturns['whitelist_organisation(address)void'] | undefined]>

  /**
   * Calls the remove_organisation(address)void ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  removeOrganisation(params?: CallParams<DxArgs['obj']['remove_organisation(address)void'] | DxArgs['tuple']['remove_organisation(address)void']>): DxComposer<[...TReturns, DxReturns['remove_organisation(address)void'] | undefined]>

  /**
   * Calls the is_whitelisted(address)bool ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  isWhitelisted(params?: CallParams<DxArgs['obj']['is_whitelisted(address)bool'] | DxArgs['tuple']['is_whitelisted(address)bool']>): DxComposer<[...TReturns, DxReturns['is_whitelisted(address)bool'] | undefined]>

  /**
   * Calls the get_org_count()uint64 ABI method.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  getOrgCount(params?: CallParams<DxArgs['obj']['get_org_count()uint64'] | DxArgs['tuple']['get_org_count()uint64']>): DxComposer<[...TReturns, DxReturns['get_org_count()uint64'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the Dx smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): DxComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): DxComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<DxComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<DxComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<DxComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<DxComposerResults<TReturns>>
}
export type DxComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

